# cargo-deny policy for markdown-delight.
#
# markdown-delight is MIT — source AND distributed binary. The pinned Zed/gpui
# graph would otherwise link a few GPL-3.0-or-later crates (ztracing / zlog) into
# the binary via gpui -> sum_tree, so patches/sever-gpl-crates.patch removes that
# edge (applied by scripts/prepare-gpui.sh). With the sever in place the graph is
# permissive-clean — no GPL exceptions are needed. This policy keeps it that way:
# any *new* copyleft crate creeping in fails the gate.

[licenses]
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "BSL-1.0",
    "bzip2-1.0.6",
    "CC0-1.0",
    "ISC",
    "MIT",
    "MITNFA",   # fmt2io (via comrak)
    "MPL-2.0",
    "NCSA",
    "Unicode-3.0",
    "Zlib",
]
confidence-threshold = 0.8

exceptions = [
    { allow = ["Unicode-DFS-2016"], name = "finl_unicode" }, # via comrak
]

# gpui sub-crates that ship without an embedded license file but are Apache-2.0.
[[licenses.clarify]]
name = "gpui_util"
expression = "Apache-2.0"
license-files = []

[[licenses.clarify]]
name = "gpui_shared_string"
expression = "Apache-2.0"
license-files = []

[bans]
# The Zed workspace pulls multiple versions of some crates; we never publish a
# crate, so allow it. Wildcard path deps (gpui via path) are expected.
multiple-versions = "allow"
wildcards = "warn"
allow-wildcard-paths = true

[advisories]
yanked = "warn"
# Unmaintained advisories: only flag markdown-delight's own first-party deps;
# the vendored Zed/gpui graph carries several (async-std, bincode, instant,
# paste, yaml-rust, …) that are upstream's to fix.
unmaintained = "workspace"
ignore = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-git = [
    "https://github.com/zed-industries/zed",
    "https://github.com/zed-industries/wgpu.git",
    "https://github.com/zed-industries/font-kit",
    "https://github.com/zed-industries/xim-rs.git",
]
